<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nara.alarm" >

    <uses-sdk android:minSdkVersion="17"/>

    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AlarmActivity">
            <intent-filter>
                <action android:name="AlarmActivity"></action>
            </intent-filter>
        </activity>
        <activity android:name=".AlarmDetailView">
            <intent-filter>
                <action android:name="AlarmDetailView"></action>
            </intent-filter>
        </activity>

        <activity android:name=".removeActivity">
            <intent-filter>
                <action android:name="AlarmRemove"></action>

            </intent-filter>
        </activity>

        <activity android:name=".AlarmDialog"
            android:theme="@android:style/Theme.Holo.Dialog">
        </activity>

        <receiver android:name=".AlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="AlarmReceiver"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>